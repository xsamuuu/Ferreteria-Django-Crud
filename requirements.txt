# Dependencias base
Django==4.2.14
gunicorn==20.1.0
# psycopg2-binary no se instalará en Windows automáticamente; en Windows use alternativas o instale PostgreSQL dev tools
psycopg2-binary==2.9.9; sys_platform != 'win32'
dj-database-url==1.0.0
whitenoise==6.5.0
python-dotenv==1.0.0
# opcional: herramientas útiles
django-environ==0.9.0

